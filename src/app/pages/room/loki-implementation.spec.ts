import { LokiImplementation } from './loki-implementation';

describe('LokiImplementation', () => {
  it('should create an instance', () => {
    expect(new LokiImplementation()).toBeTruthy();
  });
});
